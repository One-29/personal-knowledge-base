# [M4] Agent 多步工作流：任务拆解 / 逐步执行 / 缺料可见 / 汇总

> 本文件 = M4 子 Issue 内容（按 `docs/templates/module-design.md` 填写）。
> 依据：01 §3.3（US-M4-01/02）· 02 §3（M4 边界）· **05 §5（AW1–AW5 已拍板）**。

## 1. 功能定位与上下游

让用户发起**跨文档综合任务**（如"总结我在 TCP 上记过哪些知识点，逐条给出出处"）：系统自动拆解为子步骤，逐步检索加工，最终汇总为结构化结果，并显式标注哪一步缺料。

- 上游依赖：`M3 问答·溯源·拒答`（每步执行都调 M3 的检索问答，复用同一套检索参数、阈值与防幻觉校验）
- 下游被依赖：`M5 前端`（展示步骤进度与结果）

## 2. 数据原型（伪代码）

```python
class WorkflowStep:
    index: int
    goal: str                  # 该步要查什么
    query: str                 # 实际用于检索的问题（规划输出）
    status: str                # answered / insufficient / error
    conclusion: str | None     # 该步结论（answered）
    note: str | None           # 缺料或错误说明（insufficient / error）
    citations: list[Citation]  # 该步的引用

class WorkflowResult:
    task: str
    steps: list[WorkflowStep]
    answer: str                # 汇总后的最终答案
    citations: list[Citation]  # 全局统一编号（AW4）
```

## 3. 对外接口（伪代码）

```text
POST /api/v1/workflow
  请求体: {"task": str(1..500), "kb_id": int?（缺省=全库）, "max_steps"?: int(默认5)}
  200: WorkflowResult        # 含每步状态与缺料说明（同步返回，AW3）
  400: 空任务 / 知识库为空
  404: kb_id 指定的库不存在
  422: 参数校验失败
```

## 4. 模块边界对照表（唯一权威）

| 属于本模块 | 不属于（归属模块） |
|---|---|
| 任务拆解为步骤清单（LLM 规划，AW1） | 单步检索与生成（**M3**，本模块只调用） |
| 步骤循环调度、状态记录、缺料判定 | 拒答判定规则与引用越界校验（**M3**，汇总时复用） |
| 跨步汇总与结构化输出 | 切分/向量化（M2）、文档 CRUD（M1） |
| 步骤进度对用户可见（响应结构内） | 前端渲染（M5） |

**红线**：M4 不得自行实现检索或生成——否则会分裂出第二套检索逻辑，检索质量无法统一评估（02 §3）。

## 5. 验收清单（PR 合并条件）

- [ ] 规划：LLM 输出 JSON 步骤清单；解析失败退化为单步（不阻断用户）
- [ ] 步骤上限：默认 5 步，超出的步骤被截断并标注
- [ ] 执行：逐步调 M3；单步拒答记为 `insufficient`（含原因）、单步技术故障记为 `error` 且**不中断后续步骤**
- [ ] 汇总：最终答案 + **全局统一编号引用**；越界引用仍被 M3 的纯规则拦截
- [ ] 缺料可见：最终结果显式标注「第 N 步：知识库无相关内容」（US-M4-02）
- [ ] 测试：规划成功/规划失败退化/单步缺料/单步故障继续/汇总引用统一编号/空任务 422/库不存在 404
- [ ] Swagger 手工验证 `/workflow` 全流程
- [ ] PR 关联本 Issue，粒度小

---

## 台账（M4）

| 日期 | 事项 | Issue | PR |
|---|---|---|---|
| 2026-09-10 | 子 Issue 创建（设计文档 05 已拍板 AW1–AW5） | 本 Issue | — |
