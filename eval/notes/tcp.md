# TCP 三次握手

客户端发送 SYN（同步序列号），服务端回复 SYN+ACK 确认并同步自己的序号，客户端再回 ACK。
三次的意义：确认双方的发送与接收能力都正常。

## 为什么不是两次

两次只能确认客户端的发送能力与服务端的收发能力，无法确认客户端的接收能力。
所以需要第三次 ACK 来确认客户端也能收到服务端的数据。

## 四次挥手

FIN、ACK、FIN、ACK 四步。因为 TCP 是全双工，两个方向要分别关闭。
主动关闭方发送 FIN，被动方先回 ACK，等自己数据发完再发 FIN，最后主动方回 ACK。
