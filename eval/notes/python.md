# Python 装饰器

装饰器是一个接收函数并返回新函数的可调用对象，用于在不修改原函数代码的前提下增强行为。
`@decorator` 语法等价于 `func = decorator(func)`。带参数的装饰器需要三层嵌套。

## 闭包与 nonlocal

闭包是引用了外部函数变量的内层函数。修改外部变量需要 `nonlocal` 声明，
否则 Python 会把它当作内层函数的局部变量。

## 生成器与 yield

含 `yield` 的函数是生成器函数，调用它返回生成器对象而非直接执行。
生成器惰性求值，适合处理大数据流，避免一次性占用内存。
