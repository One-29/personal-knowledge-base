# 操作系统进程调度

时间片轮转：每个就绪进程轮流获得固定长度的时间片，公平但上下文切换开销大、响应时间不确定。
适用于分时系统。

## 优先级调度

每个进程有优先级，调度器总是选择优先级最高的就绪进程运行。
问题是低优先级进程可能长期得不到 CPU，即饥饿现象；解决办法是老化（aging）——随时间提升等待进程的优先级。

## 死锁的四个必要条件

互斥、占有并等待、不可剥夺、循环等待。破坏其中任意一个条件即可预防死锁。
常见策略：资源有序分配法破坏循环等待。
